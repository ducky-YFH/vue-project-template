import service from "./config";

export default {
  get(options) {
    return service(options);
  },
  post(options) {
    return service(options);
  },
  put(options) {
    return service(options);
  },
  delete(options) {
    return service(options);
  },
};
